export default function TodosLoading() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl items-center justify-center px-6">
      <p aria-live="polite" className="text-sm font-medium text-purple-800">
        할 일을 불러오는 중입니다.
      </p>
    </main>
  );
}
